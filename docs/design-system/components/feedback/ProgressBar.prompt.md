Brand-filled progress track — the canonical place the brand signals "progreso".

```jsx
<ProgressBar value={62} label="Ciclo Q2" showValue />
<ProgressBar segments={4} value={25} size="sm" />   {/* step 1 of 4 */}
```
