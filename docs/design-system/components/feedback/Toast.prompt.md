Transient confirmation. `tone="success"` uses the brand tone (white-label rule). Presentational — host controls show/hide.

```jsx
<Toast tone="success" title="Evaluación enviada" description="Gracias por tu tiempo." onClose={dismiss} />
```
