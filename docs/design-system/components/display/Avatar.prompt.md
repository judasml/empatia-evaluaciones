Person marker — image or auto initials on a warm neutral fill. Stays neutral so lists stay calm.

```jsx
<Avatar name="Lucía Fernández" size="md" status="complete" />
<Avatar src="/uploads/ana.jpg" name="Ana Ruiz" size="lg" />
```
`status="complete"` shows a brand dot (e.g. this evaluator has submitted).
