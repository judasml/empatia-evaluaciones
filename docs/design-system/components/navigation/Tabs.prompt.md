Underline tab navigation with optional counts. Active tab uses the brand underline.

```jsx
<Tabs tabs={[{value:'pend',label:'Pendientes',count:4},{value:'done',label:'Enviadas'}]} defaultValue="pend" onChange={setTab} />
```
