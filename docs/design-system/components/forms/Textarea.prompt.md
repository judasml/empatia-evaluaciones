Multi-line field for open feedback comments, with optional live character count.

```jsx
<Textarea label="Comentario abierto" rows={5} maxLength={600} showCount helper="Sé específico y constructivo." />
```
