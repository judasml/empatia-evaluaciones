Native-backed dropdown styled to match Input. Pass `options={[{value,label}]}` or `<option>` children.

```jsx
<Select label="Relación laboral" placeholder="Selecciona…" options={[{value:'peer',label:'Compañero/a'},{value:'manager',label:'Responsable'}]} />
```
